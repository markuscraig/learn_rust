Compile and run:

```
# debug
$ cargo run

# release
$ cargo run --release
```

Compile and run separately:
```
# debug
$ cargo build
$ ./target/debug/hello_cargo

# release
$ cargo build --release
$ ./target/release/hello_cargo
```
